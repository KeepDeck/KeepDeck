[package]
name = "keepdeck-index"
version = "0.1.0"
edition = "2021"
description = "Disposable SQLite+FTS5 search index over agent session stores."

[dependencies]
rusqlite = { version = "0.32", features = ["bundled"] }

[dev-dependencies]
tempfile = "3"
