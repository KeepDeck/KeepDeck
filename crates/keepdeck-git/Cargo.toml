[package]
name = "keepdeck-git"
version = "0.1.0"
edition = "2021"
description = "Git worktree provisioning for KeepDeck agent isolation."

[dependencies]
# none — shells out to the user's `git`; pure std otherwise.
