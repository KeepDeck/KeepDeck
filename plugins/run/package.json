{
  "name": "@keepdeck-plugins/run",
  "private": true,
  "version": "0.1.0",
  "description": "The Run built-in plugin: the ported Run panel — command presets, run sessions and the live log — on the plugin API's services and storage.",
  "type": "module",
  "peerDependencies": {
    "react": "^19.1.0"
  },
  "dependencies": {
    "@keepdeck/plugin-api": "workspace:*",
    "@keepdeck/terminal-kit": "workspace:*",
    "@xterm/addon-fit": "^0.10.0",
    "@xterm/xterm": "^5.5.0"
  },
  "devDependencies": {
    "@types/react": "^19.1.8",
    "typescript": "~5.8.3"
  }
}
