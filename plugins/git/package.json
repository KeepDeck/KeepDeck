{
  "name": "@keepdeck-plugins/git",
  "private": true,
  "version": "0.1.0",
  "description": "The Git built-in plugin: a live changes view (status and diffs) per worktree, on the plugin API's git service.",
  "type": "module",
  "peerDependencies": {
    "react": "^19.1.0"
  },
  "dependencies": {
    "@keepdeck/plugin-api": "workspace:*",
    "@keepdeck/ui-kit": "workspace:*"
  },
  "devDependencies": {
    "@types/react": "^19.1.8",
    "typescript": "~5.8.3"
  }
}
