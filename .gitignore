# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Claude Code local settings (personal, per-machine; shared .claude/settings.json stays tracked)
.claude/settings.local.json

# Rust / Cargo (workspace target dir lives at the repo root)
/target

# Local agent scratch / staging dir (never commit)
.agents/
