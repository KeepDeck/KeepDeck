/** Lives in the shared @keepdeck/ui-kit; re-exported so host `ui/MenuButton`
 * imports read like the rest of the shared chrome. */
export {
  MenuButton,
  type MenuAction,
  type MenuButtonProps,
} from "@keepdeck/ui-kit/MenuButton";
