/** Lives in the shared @keepdeck/ui-kit; re-exported so host `ui/Button`
 * imports read like the rest of the shared chrome. */
export {
  Button,
  type ButtonProps,
  type ButtonSize,
  type ButtonVariant,
} from "@keepdeck/ui-kit/Button";
