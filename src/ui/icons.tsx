/** The app's stroke-icon set now lives in the shared @keepdeck/ui-kit so the
 * host and built-in plugins draw from one source; this re-export keeps every
 * existing `../ui/icons` import working unchanged. */
export * from "@keepdeck/ui-kit/icons";
