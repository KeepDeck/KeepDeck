/**
 * Teams, as one thing: the model plus everything asked of it. A pure
 * re-export barrel — it DEFINES nothing, so no sibling has to import from it
 * to reach a shape or a mint.
 */
export * from "./model";
export * from "./collection";
export * from "./transforms";
