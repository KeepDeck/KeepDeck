/**
 * Settings — the global app settings document ([F6]): schema, defaults, and
 * the per-key-tolerant codec for the hand-editable settings.json (a bag of
 * independent facts — a bad value degrades only its own key).
 */
export * from "./settings";
