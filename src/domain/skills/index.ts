/**
 * Shared agent skills — the SKILL.md library authored inside KeepDeck and
 * distributed to every CLI at spawn time: naming rules and frontmatter
 * compose/parse (storage and staging live behind `src/ipc/skills.ts`).
 */
export * from "./skills";
